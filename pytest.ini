# AI-ASSISTED: Yes
# AI-TOOL: Claude Code
# PROMPT: Create pytest configuration for ArtTouch NFC project
# DATE: 2026-04-19
# ENGINEER: System
# RISK-LEVEL: P3

[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Output options
addopts =
    -v
    --tb=short
    --strict-markers
    --disable-warnings

# Markers
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    integration: marks tests as integration tests
    unit: marks tests as unit tests

# Minimum Python version
minversion = 3.10

# Test discovery
norecursedirs = .git .venv __pycache__ .pytest_cache backend frontend

# Coverage options (if pytest-cov is installed)
# addopts = --cov=backend --cov-report=term-missing --cov-report=html

[coverage:run]
branch = True
source = backend/app

[coverage:report]
exclude_lines =
    pragma: no cover
    def __repr__
    raise AssertionError
    raise NotImplementedError
    if __name__ == .__main__.:
