# Debug 执行强制清单

**目的**：阻止"习惯性绕过"和"顺手修改"，强制系统性调试流程。

**⚠️ 强制触发规则（不得跳过）**：

> 涉及代码、debug、测试任务时，**立即自动加载**以下技能，再开始任何分析：
> 1. `artouch-debugging-notes` — 包含服务器路径、数据库类型、已知错误模式
> 2. `systematic-debugging` — 四步闭环调试流程
>
> **违规**：在技能加载前动手修改代码 → 立即回滚

---

## ⚠️ 修改代码前必须完成

### Phase 1 检查（全部✅才能继续）

- [ ] 错误信息已完整阅读（stack trace + line number）
- [ ] 问题可稳定复现（步骤明确）
- [ ] 最近变更已检查（`git diff`）
- [ ] 根因已定位（不是猜测）

### 修改 Scope 声明（必须回答）

```
1. 根因是什么？ （不超过30字）
2. 只改哪个文件/函数？ 
3. 不在范围内的文件（明确不修改）：
4. 验证方法？ （测试命令）
```

---

## 🚫 强制规则

| 规则 | 违规动作 | 后果 |
|------|---------|------|
| 根因不明 | 写任何修复代码 | 回滚 |
| 修改>1个文件 | 未声明原因 | 回滚 |
| 无回归测试 | 部署 | 立即停止 |
| 3次以上fix循环 | 继续尝试修复 | 暂停，重新分析 |

---

## 🔄 每轮修复后的检查

```
修复后验证清单：
- [ ] 运行单元测试：pytest <test_path> -v
- [ ] 运行相关测试套件：pytest tests/<module>/ -q
- [ ] 验证边界情况
- [ ] 检查git diff，确认只改动目标文件
```

---

## 🚨 违规检测

### 检测0：技能未加载（最高优先级）
```
症状：涉及代码/debug任务时，artouch-debugging-notes 和 systematic-debugging 未被加载
动作：❌ 停止一切操作，立即加载两个技能，再继续
原因：无技能指引时必然跳Phase，导致习惯性绕过
```

### 检测1：顺手修改
```
症状：修复问题A时发现B问题
动作：❌ 忽略B，继续修复A
原因：B是独立任务，需要新ticket
```

### 检测2：Phase跳跃
```
症状：跳过Phase 1直接写修复代码
动作：❌ 停止，立即回到Phase 1
原因：根因不明时的修复必然引入新bug
```

### 检测3：多文件污染
```
症状：修改文件超过1个（除非明确级联）
动作：❌ 只保留必要修改，回滚其他
原因：单一问题应该对应最小改动
```

---

## 📋 使用方法

每次debug任务开始时，复制以下模板并填写：

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🔍 DEBUG任务启动
━━━━━━━━━━━━━━━━━━━━━━━━━━━━
时间：<YYYY-MM-DD HH:MM>
问题描述：<错误现象>

Phase 1 检查：
- 错误信息：<关键stack trace>
- 复现步骤：<1、2、3>
- 最近变更：<git log -3 --oneline>
- 根因假设：<不超过30字>

修改Scope：
- 目标文件：
- 目标函数：
- 不修改的文件：

验证方法：
- 测试命令：
- 预期结果：
━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

**版本**: v1.0
**创建日期**: 2026-04-23
**维护人**: Hermes Agent
