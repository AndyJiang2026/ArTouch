# 已知问题预防清单

**目的**：记录高频错误的预防检查，防止重复踩坑。

---

## 高频错误预防

### 1. FastAPI Form() vs Query() 混淆

**问题**：multipart/form-data 上传接口，非File参数必须用 `Form()` 标记

**易出错的文件**：
- `backend/app/api/v1/videos.py` - upload_video 端点
- `backend/app/api/v1/skus.py` - 批量创建SKU接口

**预防检查**：
```bash
# 检查接口是否使用 multipart/form-data
grep -n "multipart" app/api/v1/*.py

# 确认 Form() 使用
grep -n "Form()" app/api/v1/videos.py
```

---

### 2. JWT认证与RBAC权限耦合

**问题**：修改 auth.py 可能影响权限验证逻辑

**易出错的文件**：
- `backend/app/core/auth.py`
- `backend/app/api/v1/nfc_tags.py` - approve/reject 端点

**预防检查**：
```bash
# 修改 auth.py 前必须运行
pytest tests/test_auth.py -v
pytest tests/test_permissions.py -q
```

---

### 3. 数据库模型关系破坏

**问题**：修改 model 时 relationship 可能失效

**易出错的文件**：
- `backend/app/models/nfc_tag.py`
- `backend/app/models/sku.py`

**预防检查**：
```bash
# 修改 model 后必须验证
pytest tests/test_models.py -v
python -c "from app.models import *; print('Models OK')"
```

---

### 4. Vue编译后dist不同步

**问题**：修改Vue源码后忘记重新编译

**易出错的文件**：
- `frontend/src/` 下所有 .vue 文件

**预防检查**：
```bash
# 部署前必须确认
cd frontend && npm run build
# 验证 dist 目录时间戳
ls -la ../dist/assets/
```

---

### 5. 服务器文件未同步

**问题**：本地修改后服务器没有 rsync 同步

**易出错的场景**：
- 部署到 39.105.87.26
- systemd 服务未重载

**预防检查**：
```bash
# 部署命令模板
rsync -avz --exclude='venv' --exclude='node_modules' \
  ./backend/ root@39.105.87.26:/root/ArTouch/backend/
fuser -k 8000/tcp  # 重启前必须
```

---

### 6. NFC TagClick 级联删除

**问题**：删除 NFC Tag 时未清理关联数据

**易出错的文件**：
- `backend/app/api/v1/nfc_tags.py` - delete endpoint

**预防检查**：
```bash
# 删除操作前验证外键关系
pytest tests/test_nfc_tags.py::test_delete_cascade -v
```

---

### 7. Status 字段类型错误

**问题**：数据库 status 是 string，代码传入 int 导致错误

**易出错的文件**：
- `backend/app/schemas/nfc_tag.py`
- `backend/app/api/v1/nfc_tags.py`

**预防检查**：
```python
# 确认 status 类型一致
status: str  # 而非 int
```

---

### 8. 服务器同时运行两个数据库（SQLite + PostgreSQL）

**问题**：服务器存在SQLite和PostgreSQL两个数据库，后端服务实际使用PostgreSQL，修改SQLite无效

**易出错的场景**：
- 重置密码、查询数据时只改了SQLite
- 确认后端运行状态时用了错误的数据库路径

**预防检查**：
```bash
# Step 1: 找到后端进程PID
ps aux | grep uvicorn | grep -v grep

# Step 2: 确认该进程使用的数据库
cat /proc/<PID>/environ | tr '\0' '\n' | grep DATABASE

# 实际输出示例：
# DATABASE_URL=postgresql://artouch:***@localhost:5432/artouch  ← 实际用的是这个
# 而不是: sqlite:///data/artouch.db
```

---

### 9. Schema与Model的NULL处理不一致

**问题**：数据库字段为NULL时，Pydantic schema验证失败返回500

**易出错的文件**：
- `app/schemas/nfc_tag.py` - `NFCTagResponse`
- `app/schemas/sku.py`

**预防检查**：
```bash
# 数据库字段为NULL但schema要求非空时的错误信息：
# "Input should be a valid integer, input is None"

# 修复：schema中 nullable 字段必须写成 int | None，不是 int
grep -n "video_id: int" app/schemas/nfc_tag.py
# 如果数据库允许NULL，必须写成 video_id: int | None
```

修改上述易出错文件前，**必须**执行对应预防检查。

如果发现新的高频错误，**立即**添加到此清单。

---

**版本**: v1.0
**创建日期**: 2026-04-23
**相关文件**：
- `.debug-enforcement.md` - Debug执行清单
- `.error-log.md` - 错误模式日志
- `.debug-preflight.md` - 修改前检查
