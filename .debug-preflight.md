# 修改前必须回答的问题

**目的**：在动手修改前强制思考，防止冲动修复。

---

## 🔴 Q0: 环境确认（修改数据库前必做）

**如果涉及数据库修改，必须先回答：**

```bash
# 1. 找到运行中的进程PID
ps aux | grep uvicorn | grep -v grep

# 2. 确认该进程使用的数据库类型
cat /proc/<PID>/environ | tr '\0' '\n' | grep DATABASE

# 3. 确认修改目标正确
# PostgreSQL: postgresql://...
# SQLite: sqlite:///...
```

**违规：修改数据库前未确认 DATABASE_URL → 立即回滚**

---

## 5个问题（Q0-Q4）

### Q1: 根因是什么？

**限制：不超过50字**

回答：_______________

检查：如果回答是"可能是X"或"大概是Y"，说明根因不明，必须继续分析。

---

### Q2: 最小改动是什么？

**只列出改动的文件/函数，不超过3处**

1. 文件：__________ 函数：__________
2. 文件：__________ 函数：__________
3. 文件：__________ 函数：__________

检查：如果超过3处，暂停，重新评估是否过度修复。

---

### Q3: 如何验证修复有效？

**必须能运行测试命令证明**

测试命令：_______________

预期结果：_______________

检查：如果无法用测试证明，说明修复无法验证，禁止部署。

---

### Q4: 如何防止回归？

**必须能写出测试用例**

测试用例位置：_______________

检查：如果无法写出回归测试，说明修复是危险的，考虑是否值得。

---

## 🚫 不回答这些问题 = 不动手

```
Q0 环境确认：_______________(如不涉及数据库则填"N/A")
Q1 根因：_______________
Q2 最小改动：___________（X处）
Q3 验证方法：___________
Q4 防止回归：___________

→ 禁止修改代码
```

---

## ✅ 完成后
```
✅ 修改前检查完成
Q0 环境确认：<已填写/不适用>
Q1 根因：<已填写>
Q2 最小改动：<已填写，X处>
Q3 验证方法：<已填写>
Q4 防止回归：<已填写>

→ 可以开始修改
```

---

**版本**: v1.1
**创建日期**: 2026-04-23
**维护人**: Hermes Agent
