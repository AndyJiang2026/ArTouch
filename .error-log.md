# 错误模式日志

**目的**：追踪重复错误，建立预防机制，打破"同样的错误反复犯"的循环。

---

## 如何使用

1. 发现重复错误时，在此记录
2. 分析根因和教训
3. 制定预防措施
4. 更新 `artouch-debugging-notes` 技能

---

## 重复错误记录

### [模板]
```markdown
### 错误模式 #N: <简短标题>
- 日期：YYYY-MM-DD
- 问题：<具体错误现象>
- 根因：<为什么会犯这个错误>
- 教训：<从中学到什么>
- 预防措施：<下次如何避免>
- 相关文件：<易出问题的文件>
```

---

## 重复错误记录

### 错误模式 #1: 服务器有两个数据库，修改了错误的数据库
- 日期：2026-04-23
- 问题：重置admin密码后登录仍失败，显示"用户名或密码错误"
- 根因：服务器同时运行SQLite和PostgreSQL两个数据库，后端实际使用PostgreSQL，但修改了SQLite
- 教训：修改密码前必须先确认当前服务使用的是哪个数据库
- 预防措施：先用 `cat /proc/<PID>/environ | grep DATABASE` 确认数据库类型
- 相关文件：`/root/ArTouch/backend/`（实际后端）、`/home/admin/artouch/backend/`（旧代码）

### 错误模式 #2: Schema未处理数据库NULL值
- 日期：2026-04-23
- 问题：NFC标签列表API返回500，错误"Input should be a valid integer, input is None"
- 根因：`NFCTagResponse.video_id: int` 但数据库中有些记录video_id为NULL，Pydantic验证失败
- 教训：修改模型nullable=True后，必须同步修改Pydantic schema；数据库约束和ORM模型是两层独立的
- 预防措施：数据库字段变更时，同步检查所有使用该字段的schema
- 相关文件：`/root/ArTouch/backend/app/schemas/nfc_tag.py`

### 错误模式 #3: 有机制但无强制触发
- 日期：2026-04-23
- 问题：`artouch-debugging-notes`技能存在，但debug任务中未主动加载，导致重复踩坑
- 根因：机制存在但依赖用户提醒才加载，缺乏自动触发
- 教训：技能必须在任务上下文匹配时自动加载，不能靠外部提醒
- 预防措施：建立强制触发规则——涉及代码/debug时，自动加载技能；动手修改代码前必须确认技能已加载
- 相关文件：`.debug-enforcement.md`（本文件）、`RULES.md`

---

**版本**: v1.0
**创建日期**: 2026-04-23
**维护人**: Hermes Agent
